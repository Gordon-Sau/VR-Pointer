using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PhysicsPointer : MonoBehaviour
{
//     public float maxLen = 3.0f;
//     private LineRenderer lineRenderer = null;
//     // Start is called on the frame when a script is enabled just before any of  Update methods are called
//     // Awake is called when the script instance is being loaded, even before Start. It is usually used to set up reference to other objects.
//     void Awake()
//     {
//         lineRenderer = GetComponent<LineRenderer>();
//     }

//     // Update is called once per frame
//     void Update()
//     {
//         lineRenderer.SetPosition(0, transform.position);
//         lineRenderer.SetPosition(1, getEnd());
//     }

//     private Vector3 getEnd() {
//         RaycastHit hit;
//         if (Physics.Raycast(transform.position, transform.forward, out hit, maxLen)) {
//             return hit.point;
//         } else {
//             return transform.position + transform.forward * maxLen;
//         }
//     }

}
